# Dependencies for generate_report.py, which rebuilds the static GitHub Pages
# site in docs/ (run by .github/workflows/update.yml on a schedule).
#
# The full development set (fastapi, duckdb, pytest, anthropic, ...) lives in
# pyproject.toml; install it locally with:  pip install -e ".[dev,app,llm]"
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
hmmlearn>=0.3
fredapi>=0.5
pyyaml>=6.0
plotly>=5.18
requests>=2.31

# Kept so the legacy Streamlit dashboard (app/streamlit_app.py) still deploys
# from this file; generate_report.py does not import it.
streamlit>=1.32
